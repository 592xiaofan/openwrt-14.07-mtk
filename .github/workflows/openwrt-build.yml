name: Build OpenWrt 14.07-mtk for MT7688 (基础固件)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout source (当前仓库)
        uses: actions/checkout@v4
        with:
          path: openwrt
          fetch-depth: 1

      - name: Create ccache directory
        run: mkdir -p ${{ github.workspace }}/ccache

      - name: Build in Ubuntu 12.04 container
        id: build
        run: |
          docker run --name openwrt-build \
            -e TZ=Asia/Shanghai \
            -v ${{ github.workspace }}/openwrt:/openwrt \
            -v ${{ github.workspace }}/ccache:/ccache \
            ubuntu:12.04 /bin/bash -c "
            set -exo pipefail

            # 更换为旧版本软件源
            sed -i 's/archive.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list
            apt-get update
            apt-get install -y build-essential subversion libncurses5-dev zlib1g-dev gawk gcc-multilib flex git-core gettext libssl-dev unzip python2.7 file wget curl ccache

            # 验证 GCC 版本 (应为 4.6.3)
            echo '=== GCC version ==='
            gcc --version | head -1

            # 配置 ccache
            export PATH=/usr/lib/ccache:\$PATH
            export CCACHE_DIR=/ccache
            ccache -M 10G

            cd /openwrt

            # 更新 feeds
            ./scripts/feeds update -a
            ./scripts/feeds install -a

            # 生成 .config 文件（针对 MT7688）
            cat > .config << 'EOF'
            CONFIG_TARGET_ramips=y
            CONFIG_TARGET_ramips_mt76x8=y
            CONFIG_TARGET_ramips_mt76x8_DEVICE_mt7688=y
            CONFIG_TARGET_OPTIONS=y
            CONFIG_TARGET_OPTIONS_BAUDRATE_115200=y
            CONFIG_KERNEL_BRIDGE=y
            CONFIG_KERNEL_VLAN_8021Q=y
            CONFIG_PACKAGE_dnsmasq=y
            CONFIG_PACKAGE_iptables=y
            CONFIG_PACKAGE_iptables-mod-nat=y
            CONFIG_PACKAGE_kmod-rt2800-soc=y
            CONFIG_PACKAGE_wpad-mini=y
            CONFIG_PACKAGE_hostapd-mini=y
            CONFIG_PACKAGE_kmod-usb-core=y
            CONFIG_PACKAGE_kmod-usb-ohci=y
            CONFIG_PACKAGE_kmod-usb-storage=y
            CONFIG_PACKAGE_kmod-usb2=y
            CONFIG_PACKAGE_kmod-sdio=y
            CONFIG_PACKAGE_kmod-mmc=y
            CONFIG_PACKAGE_kmod-tun=y
            CONFIG_PACKAGE_kmod-sched=y
            CONFIG_PACKAGE_luci-app-qos=y
            CONFIG_PACKAGE_ddns-scripts=y
            CONFIG_PACKAGE_luci-app-ddns=y
            CONFIG_PACKAGE_luci=y
            CONFIG_PACKAGE_luci-mod-admin-full=y
            CONFIG_TARGET_ROOTFS_SQUASHFS=y
            CONFIG_PACKAGE_ipv6=n
            CONFIG_PACKAGE_kmod-ipv6=n
            CONFIG_PACKAGE_samba36=n
            CONFIG_PACKAGE_miniupnpd=n
            EOF

            echo '========== .config =========='
            cat .config
            echo '=============================='

            make defconfig

            # 下载源码包
            make download V=s

            # 编译
            CPUS=\$(nproc)
            [ \$CPUS -gt 4 ] && CPUS=4
            make -j\$CPUS V=s 2>&1 | tee build.log

            # 检查固件是否生成
            if ! find bin/ -name '*.bin' -o -name '*.trx' | grep -q .; then
                echo 'ERROR: No firmware files found!'
                exit 1
            fi

            echo '========== Firmware files =========='
            find bin/ -name '*.bin' -o -name '*.trx' | xargs ls -lh
            echo '====================================='
            "

      - name: Copy artifacts from container
        if: always()
        run: |
          mkdir -p ${{ github.workspace }}/firmware ${{ github.workspace }}/logs
          docker cp openwrt-build:/openwrt/bin/. ${{ github.workspace }}/firmware/ || echo "Failed to copy firmware"
          docker cp openwrt-build:/openwrt/build.log ${{ github.workspace }}/logs/ || echo "Failed to copy build.log"
          docker cp openwrt-build:/openwrt/.config ${{ github.workspace }}/logs/config.txt || echo "Failed to copy .config"
          docker cp openwrt-build:/openwrt/feeds.conf ${{ github.workspace }}/logs/feeds.conf 2>/dev/null || true
          sudo chown -R $USER:$USER ${{ github.workspace }}/firmware ${{ github.workspace }}/logs

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-14.07-mt7688-basic
          path: firmware/
          retention-days: 30
          if-no-files-found: warn

      - name: Upload build logs (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: logs/
          retention-days: 30

      - name: Cache ccache
        uses: actions/cache@v3
        with:
          path: ccache
          key: ccache-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            ccache-${{ runner.os }}-
